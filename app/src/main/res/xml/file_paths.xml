<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Define path for internal files directory -->
    <files-path
        name="internal_files"
        path="." />
    <!-- Define path specifically for note images within the internal files directory -->
    <files-path
        name="note_images"
        path="note_images/" /> <!-- Path used in ViewModel -->
    <!-- Define path specifically for imported images (if keeping separate, otherwise remove) -->
    <files-path
        name="imported_images"
        path="imported_images/" /> <!-- Path used in import logic (consider unifying) -->
    <!-- 如果将来需要共享缓存文件 -->
    <!-- <cache-path name="shared_cache" path="." /> -->
    <!-- 如果将来需要共享外部存储文件 (需要相应权限) -->
    <!-- <external-path name="external_files" path="." /> -->
</paths>
